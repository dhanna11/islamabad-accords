{
  "name": "islamabad-accords-tests",
  "private": true,
  "type": "module",
  "description": "Smoke test for the built slideshow (index.html). Run from this folder: npm ci && npx playwright install chromium && node smoke.mjs",
  "scripts": { "test": "node smoke.mjs" },
  "devDependencies": { "playwright": "1.56.1" }
}
